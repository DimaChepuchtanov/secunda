# API управления инцидентами

Надежный и масштабируемый RESTful API для управления инцидентами, построенный на FastAPI и PostgreSQL. Это приложение предоставляет полный набор операций CRUD для отслеживания инцидентов, с функциями фильтрации по статусу, валидации входных данных и асинхронных взаимодействий с базой данных.

## 🚀 Возможности

- **Полные операции CRUD**: Создание, чтение, обновление и удаление инцидентов
- **Фильтрация по статусу**: Получение инцидентов по определенному статусу
- **Валидация входных данных**: Комплексная валидация данных инцидентов с использованием Pydantic
- **Асинхронная база данных**: Эффективные асинхронные операции с SQLAlchemy и asyncpg
- **Контейнеризация Docker**: Легкое развертывание с Docker и docker-compose
- **Автоматически генерируемая документация**: Интерактивная документация API через встроенный Swagger UI FastAPI

## 📋 Конечные точки API

### Получить все инциденты
**GET** `/incident/`

Получает все инциденты из базы данных.

**Пример ответа:**
```json
[
  {
    "id": "123e4567-e89b-12d3-a456-426614174000",
    "text": "Серверный сбой в продакшене",
    "status": "in work",
    "author": "operator",
    "created_at": "2023-10-01 14:30:00"
  }
]
```

### Получить инциденты по статусу
**GET** `/incident/status={status}`

Получает инциденты, отфильтрованные по определенному статусу.

**Параметры:**
- `status` (string): Статус инцидента (`created`, `checked`, `in work`, `closed`)

**Пример запроса:**
```
GET /incident/status=in work
```

**Пример ответа:**
```json
[
  {
    "id": "123e4567-e89b-12d3-a456-426614174000",
    "text": "Серверный сбой в продакшене",
    "status": "in work",
    "author": "operator",
    "created_at": "2023-10-01 14:30:00"
  }
]
```

### Получить инцидент по ID
**GET** `/incident/{incident_id}`

Получает конкретный инцидент по его уникальному ID.

**Параметры:**
- `incident_id` (string): UUID инцидента

**Пример ответа:**
```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "text": "Серверный сбой в продакшене",
  "status": "in work",
  "author": "operator",
  "created_at": "2023-10-01 14:30:00"
}
```

### Создать инцидент
**POST** `/incident/`

Создает новый инцидент.

**Тело запроса:**
```json
{
  "text": "Обнаружена новая проблема с сервером",
  "author": "monitoring"
}
```

**Пример ответа:**
```json
{
  "id": "456e7890-e89b-12d3-a456-426614174001",
  "text": "Обнаружена новая проблема с сервером",
  "status": "created",
  "author": "monitoring",
  "created_at": "2023-10-01 15:45:00"
}
```

### Обновить инцидент
**PATCH** `/incident/{incident_id}`

Обновляет существующий инцидент.

**Параметры:**
- `incident_id` (string): UUID инцидента

**Тело запроса:**
```json
{
  "text": "Обновленное описание проблемы с сервером",
  "author": "operator",
  "status": "checked"
}
```

**Пример ответа:**
```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "text": "Обновленное описание проблемы с сервером",
  "status": "checked",
  "author": "operator",
  "created_at": "2023-10-01 14:30:00"
}
```

### Удалить инцидент
**DELETE** `/incident/{incident_id}`

Удаляет инцидент по его ID.

**Параметры:**
- `incident_id` (string): UUID инцидента

**Ответ:** 204 Нет содержимого

## 🛠 Установка и настройка

### Предварительные требования
- Docker и docker-compose установлены в вашей системе

### Запуск с Docker Compose

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd incident-management-api
   ```

2. **Запустите сервисы:**
   ```bash
   docker-compose up --build
   ```

   Эта команда выполнит:
   - Сборку контейнера приложения
   - Запуск базы данных PostgreSQL
   - Автоматический запуск миграций базы данных
   - Запуск приложения FastAPI на порту 8000

3. **Доступ к API:**
   - Базовый URL API: `http://localhost:8000`
   - Интерактивная документация: `http://localhost:8000/docs`
   - Альтернативная документация: `http://localhost:8000/redoc`

### Переменные окружения

Приложение использует следующие переменные окружения (настроены в docker-compose.yml):

- `PG_USER`: Имя пользователя PostgreSQL
- `PG_PASSWORD`: Пароль PostgreSQL
- `PG_HOST`: Хост PostgreSQL
- `PG_PORT`: Порт PostgreSQL
- `PG_DB`: Имя базы данных PostgreSQL

## 📁 Структура проекта

```
incident-management-api/
├── main.py                 # Точка входа приложения FastAPI
├── requirements.txt        # Зависимости Python
├── setting.py             # Настройки и конфигурация приложения
├── Dockerfile             # Конфигурация контейнера Docker
├── docker-compose.yml     # Настройка мультиконтейнера
├── setup.cfg             # Конфигурация установки
├── repository/           # Слой доступа к данным
│   ├── __init__.py
│   ├── model.py          # Модели SQLAlchemy
│   ├── incedent.py       # Репозиторий инцидентов (примечание: опечатка в имени файла)
│   └── session.py        # Управление сессиями базы данных
├── router/               # Обработчики маршрутов API
│   ├── __init__.py
│   └── incident.py       # Конечные точки API инцидентов
├── schems/               # Схемы Pydantic
│   ├── __init__.py
│   └── incident.py       # Модели данных инцидентов
├── server/               # Слой бизнес-логики
│   ├── __init__.py
│   └── incident.py       # Логика сервиса инцидентов
└── untils/               # Утилиты
    ├── __init__.py
    └── exceptions/
        └── incident.py   # Пользовательские исключения
```

## 🔧 Разработка

### Настройка локальной разработки

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Настройте базу данных PostgreSQL** (или используйте Docker для локальной БД)

3. **Настройте переменные окружения** в `setting.py` или через окружение

4. **Запустите приложение:**
   ```bash
   uvicorn main:app --reload
   ```

## 📚 Используемые технологии

- **FastAPI**: Современный, быстрый веб-фреймворк для создания API
- **SQLAlchemy**: Инструментарий SQL и ORM для Python
- **asyncpg**: Быстрый драйвер PostgreSQL для asyncio
- **Pydantic**: Валидация данных и управление настройками
- **PostgreSQL**: Надежная реляционная база данных
- **Docker**: Платформа контейнеризации
- **Uvicorn**: ASGI-сервер для FastAPI
